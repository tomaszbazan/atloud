class TimerData {
  final bool screenLockValue;
  final Duration startingTimerValue;
  final int periodValue;
  final bool continuationAfterAlarmValue;
  
  TimerData(this.screenLockValue, this.startingTimerValue, this.periodValue, this.continuationAfterAlarmValue);
}
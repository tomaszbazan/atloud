class TimerUserPreferences {
  final bool screenLockValue;
  final Duration startingTimerValue;
  final int periodValue;
  final bool continuationAfterAlarmValue;
  
  TimerUserPreferences(this.screenLockValue, this.startingTimerValue, this.periodValue, this.continuationAfterAlarmValue);
}